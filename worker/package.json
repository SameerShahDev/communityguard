{
  "name": "discord-webhook",
  "version": "1.0.0",
  "description": "Discord webhook handler for CommunityGuard",
  "main": "api/webhook.js",
  "scripts": {
    "start": "node api/webhook.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "express": "^4.18.2",
    "tweetnacl": "^1.0.3"
  }
}
